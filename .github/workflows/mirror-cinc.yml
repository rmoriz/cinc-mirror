name: Mirror Cinc Packages to GHCR
on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      min_version:
        description: 'Minimum version to mirror (default: 18)'
        required: false
        default: '18'

jobs:
  mirror:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Make scripts executable
      run: |
        chmod +x mirror-cinc.sh
        chmod +x security-incident-response.sh

    - name: Install dependencies
      run: |
        echo "📦 Installing required dependencies..."

        # Install ORAS for OCI artifact management
        echo "Installing ORAS..."

        # Use a known working version
        ORAS_VERSION="v1.2.0"
        echo "Using ORAS version: $ORAS_VERSION"

        # Download and install ORAS with error handling
        if ! curl -f -L -o oras_linux_amd64.tar.gz "https://github.com/oras-project/oras/releases/download/${ORAS_VERSION}/oras_linux_amd64.tar.gz"; then
          echo "Failed to download ORAS, trying alternative method..."
          # Try alternative download method
          curl -f -L -o oras "https://github.com/oras-project/oras/releases/download/${ORAS_VERSION}/oras_linux_amd64"
          chmod +x oras
          sudo mv oras /usr/local/bin/
        else
          # Extract the tar.gz
          mkdir -p oras-install/
          tar -zxf oras_linux_amd64.tar.gz -C oras-install/
          sudo mv oras-install/oras /usr/local/bin/
          rm -rf oras-install/ oras_linux_amd64.tar.gz
        fi

        # Verify installations
        echo "Verifying installations..."
        if command -v oras >/dev/null 2>&1; then
          oras version
          echo "✅ ORAS installed successfully"
        else
          echo "❌ ORAS installation failed"
          exit 1
        fi

        echo "✅ All dependencies installed successfully"

    - name: Set mirror configuration
      id: config
      run: |
        # Set default values
        MIN_VERSION="18"

        # Override with input if provided
        if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.min_version }}" ]; then
          MIN_VERSION="${{ github.event.inputs.min_version }}"
        fi

        echo "min_version=$MIN_VERSION" >> $GITHUB_OUTPUT
        echo "Using minimum version: $MIN_VERSION"

    - name: Run Cinc mirror script
      env:
        GHCR_ORG: ${{ github.repository_owner }}
        GHCR_REPO: cinc-packages
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        MIRROR_DIR: ./cinc-mirror
        MIN_VERSION: ${{ steps.config.outputs.min_version }}
        TARGET_DISTROS: debian ubuntu
        FTP_BASE: ftp://downloads.cinc.sh/pub/cinc/files/stable/cinc
      run: |
        echo "🚀 Starting Cinc mirror process..."
        echo "GHCR_ORG: $GHCR_ORG"
        echo "MIN_VERSION: $MIN_VERSION"
        echo "TARGET_DISTROS: $TARGET_DISTROS"

        # Run the mirror script
        if ./mirror-cinc.sh; then
          echo "✅ Mirror process completed successfully"
        else
          echo "❌ Mirror process failed"
          exit 1
        fi
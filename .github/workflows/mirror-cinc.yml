name: Mirror Cinc Packages to GHCR
on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      min_version:
        description: 'Minimum version to mirror (default: 18)'
        required: false
        default: '18'

jobs:
  mirror:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Test mirror workflow
      run: |
        echo "🧪 Testing Cinc mirror workflow..."
        echo "This is a simplified test to verify the workflow runs correctly."
        echo ""
        echo "✅ Workflow executed successfully!"
        echo "Next step: Gradually add back the full mirror functionality."